# 情景生成 Agent

本项目是一个可以根据用户提供的文本生成图片或视频的 Agent。

## 如何使用

1. 安装依赖：
```bash
pip install -r requirements.txt
```

2. 运行 Agent：
```bash
python agent.py
```

Agent 将会根据 `agent.py` 中硬编码的提示词生成一张图片和一段视频。你可以修改 `main` 函数中的提示词来生成不同的内容。
