# 旅游行程规划 Agent

本项目是一个旅游行程规划师 Agent，可以根据用户的需求，规划出包含自然景点、人文景点和当地美食的旅游行程。

## 如何使用

1. 安装依赖：
```bash
pip install -r requirements.txt
```

2. 启动服务：
   进入到 `beginner` 目录，然后执行以下命令：
```bash
veadk web
```

3. 在浏览器中打开 `http://127.0.0.1:8000`，然后在 `agent` 对话框中选择 `travel_concierge` 即可开始体验。
